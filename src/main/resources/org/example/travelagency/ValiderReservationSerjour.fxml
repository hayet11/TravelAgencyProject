<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane xmlns:fx="http://javafx.com/javafx"
           xmlns="http://javafx.com/javafx/8"
           fx:controller="org.example.travelagency.VerificationReservationSejourController"
           style="-fx-background-color: #f4f4f4;">

    <children>
        <VBox alignment="CENTER" spacing="15" style="-fx-padding: 20;">
            <!-- Titre -->
            <Label text="Vérification de la Réservation"
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #333;" />

            <!-- Nom de l'hôtel -->
            <HBox spacing="10">
                <Label text="Nom de l'Hôtel :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="nomHotelLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Adresse -->
            <HBox spacing="10">
                <Label text="Adresse :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="adresseLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Nombre d'étoiles -->
            <HBox spacing="10">
                <Label text="Nombre d'Étoiles :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="nbrEtoilesLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Tarif par nuit -->
            <HBox spacing="10">
                <Label text="Tarif par Nuit (€) :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="tarifParNuitLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Nombre de participants -->
            <HBox spacing="10">
                <Label text="Nombre de Participants :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="nbrParticipantsLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Date d'arrivée -->
            <HBox spacing="10">
                <Label text="Date d'Arrivée :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="dateArriveeLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Nombre de nuitées -->
            <HBox spacing="10">
                <Label text="Nombre de Nuitées :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="nbrNuiteesLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Mode de paiement -->
            <HBox spacing="10">
                <Label text="Mode de Paiement :" style="-fx-font-size: 14px; -fx-text-fill: #555;" />
                <Label fx:id="modePaiementLabel" style="-fx-font-size: 14px; -fx-text-fill: #000;" />
            </HBox>

            <!-- Prix total -->
            <HBox spacing="10">
                <Label text="Prix Total (€) :" style="-fx-font-size: 16px; -fx-text-fill: #d9534f; -fx-font-weight: bold;" />
                <Label fx:id="prixTotalLabel" style="-fx-font-size: 16px; -fx-text-fill: #d9534f; -fx-font-weight: bold;" />
            </HBox>

            <!-- Boutons -->
            <HBox alignment="CENTER" spacing="20" style="-fx-padding: 10;">
                <Button fx:id="confirmerButton" text="Confirmer"
                        style="-fx-background-color: #28a745; -fx-text-fill: white; -fx-border-radius: 5px; -fx-padding: 5 15;" />
                <Button fx:id="retourButton" text="Retour"
                        style="-fx-background-color: #dc3545; -fx-text-fill: white; -fx-border-radius: 5px; -fx-padding: 5 15;" />
            </HBox>
        </VBox>
    </children>
</StackPane>
